/**
 * Reexporta o Hero do design system VOAR OS (components/voar-os/hero.tsx).
 *
 * A implementação vive no design system de propósito — este componente é
 * genérico o bastante para ser reutilizado por qualquer tela futura da
 * plataforma, não apenas pela Home. Este arquivo existe para manter a
 * estrutura de pastas prevista para features/home/components.
 */
export { Hero } from "@/components/voar-os";
